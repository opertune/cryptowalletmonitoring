{% extends 'base.html.twig' %}
{% block head %}
	<link rel="icon" href="{{asset('images/favicons/privacy.png')}}" type="image/x-icon">
	<meta name="description" content="{{ 'meta'|trans({}, 'privacyPolicy') }}">
{% endblock %}

{% block title %}{{ 'title'|trans({}, 'privacyPolicy') }}{% endblock %}

{% block body %}
	<div class="container text-white mt-5">
                <h3 class="mb-2 text-center">{{ 'body.title'|trans({}, 'privacyPolicy') }}</h3>
                {{ 'body.p'|trans({}, 'privacyPolicy')|raw }}
	</div>
{% endblock %}